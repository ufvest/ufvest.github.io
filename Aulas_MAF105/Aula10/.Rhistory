#-----------------------------------------------
plot(density(runif(25, -1, 1)))
#-----------------------------------------------
(function(x) sapply(c(1,0.1), function(a) plot(density(x, adjust=a))))(runif(25, -1, 1))
